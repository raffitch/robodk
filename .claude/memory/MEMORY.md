# Memory index

- [Paired roll capture](paired-roll-capture.md) — 2026-09-01: one press = vertical + camera rolled 90°, one trip out, default ON. Task page `docs/paired-roll-capture-handoff.md`. Verify a pair from T_work_camera (tilt 0.00 on BOTH, same xyz, 90° baseline shift) — a label is never proof. **A commanded roll lands on A4, not A6** (measured −102.54° vs +3.97°): our own |ΔA4| cap, not the arm, was refusing it; margin 15→60 in `a61dcda`.

- [Ring-2 dropout: spatial filter cleared](ring2-dropout-spatial-filter-cleared.md) — 2026-09-01: ScanAtRing2 IS the automatic pose (0.000 mm) and spatial=0 made things worse; what's real is a reproducible ~2.3x anisotropy about the stereo baseline. Only the ROLL can still decide camera-vs-scene.

- [path_completeness is not stable](path-completeness-not-stable.md) — swings 0.253-0.891 on identical scenes because the circle fit is under-constrained on a gappy ring; judge levers on ROI coverage, never one take's completeness.

- [Measure job uncancellable hang](measure-job-uncancellable-hang.md) — 2026-09-01: a taught-target capture wedged the SHARED RoboDK connection; health still says ok, `station-options` blocking is the real test, and `_wait_program` has no timeout so /cancel cannot land.

- [Raster-free centreline beats NKSR](raster-free-centreline-beats-nksr.md) — 2026-08-31: the cheap control won; radius sd 0.074→0.047 mm, 11x on the partial ring, so no reconstructor is needed for measurement. Trueness still unsettled (0.34 mm).

- [Paper timeline not a constraint](paper-timeline-not-a-constraint.md) — operator (2026-08-31): don't sequence engineering around the 1 Sep paper deadline; rescan/reprocess for the paper afterwards; measurement-integrity rules stay.

- [Deposit segmentation spec+plan](deposit-segmentation-spec-plan.md) — SHIPPED, merged to main `23d5bf3` (2026-08-31). Chroma gate deleted; fitted substrate + derived floor + compactness. 8/8 valid, radius sigma 0.074 with no colour at all — but offline only, NO ROBOT HAS MOVED. First live take: read `report["substrate"]` against the frozen golden table.

- [Calibration improvements status](calibration-improvements-status.md) — 4 phases + target-gen trio + collision-safety (baseline-relative screening + modeled platform keep-out) pushed to origin/`calibration-improvements` (`725e9dd`). Part 2 HUD tilt-DIRECTION guidance (`790bfde`) + physical-robot auto-connect (`26c3091`) + Step-0 camera-intrinsics UI panel (`ddcbe6c`) all DONE + pushed to origin/`calibration-improvements` (HEAD `ddcbe6c`, 133 tests green, build clean). TODO: real-board tilt-sign check on cell; generation-drop low poses for target 9; verify both new features on the real KUKA.
- [Scan chain audit 2026-08-14](scan-chain-audit-2026-08-14.md) — `docs/scan-audit-2026-08-14.md` is the analysis of record; headline: Jetson aligns with FACTORY intrinsics, host back-projects with CALIBRATED ones = +2.05% lateral scale the characterization can't see.
- [Camera intrinsics & distortion](camera-intrinsics-distortion.md) — D435i RGB factory distortion is all-zeros; measured value lives in tasni.config.json; config needs restart, runs can't re-solve offline.
- [Scan module status](scan-module-status.md) — module #2 (table scan → TSDF mesh + work frame/rectangle). 2026-06-26: coverage dots now mark REAL measured depth on a fixed image grid (dots stop dancing) — `667e19b` branch / `3f0a967` main, **deployed+verified on Jetson**; surface-ready flicker debounced (`93c15fc`, frontend-only). Rectangle now HUGS the board via density-cliff per-edge trim: host lock/insert (`1743d11`, branch) + Jetson live box with a COLOUR edge-veto so it can't amputate under-captured edges (`0c83bbc` branch / `f88b44d` main, **deployed+verified on Jetson**, pure logic in `server/scan_overlay.py`). Gates kept on raw extent (no regression). **2026-06-26 design pivot (UNCOMMITTED on branch, 180 tests green):** hybrid work region — keep the detected board rectangle when fully framed, else project a **generic fixed 1.0×1.0 m square on the plane centred on the reticle** (`scan.work_crop_mm`); + fixed the lock-dots "smash to centre" (freeze the live coverage union at lock). LIVE square needs the server redeployed to the Jetson. **2026-07-02 (`6d5d72c`, pushed, frontend/host-only, 192 tests green):** that whole pivot batch is now COMMITTED and the **tilt jitter is fixed** — the client freeze now also latches `tilt_deg/b/c/yaw_a` (they were left live, so TILT/LEVEL wiggled while the rectangle+dots were frozen), and `surfaceShifted` now releases the freeze on a real leveling rotation (1.5° deadband) so rectangle+tilt reproject when you move but hold still on noise. **2026-07-06 three scan defects FIXED (`d4e56bd`, pushed, host+server, Jetson auto-deployed, LIVE-VERIFIED):** (1) live overlay now draws the density-TRIMMED rectangle (server emits `trimmed_corners_color_mm`, host projects it calibrated) so it matches lock/insert; (2) reference mode wired (`generate_scan_targets`→`_reference_locate`, was dead code); (3) EDGE A lamp now populated (advisory: real for elongated platform via `edge_gate_min_aspect`, OK for near-square/crop, never blocks lock). **2026-07-06 parked-jitter FIXED (`55f3c27`, pushed, backend-only, LIVE-VERIFIED incl. real-robot A6 move):** the pose-hold toggled on noise (no release debounce; vision-escape tilt 4° below the ~6° noise) → "static then jump"; added symmetric hysteresis (`live_hold_release_frames=2`), raised tilt-escape 4→10, loosened pose tol to driver-dither level, re-anchor only while live → parked = held 124/124, 0.00 p-p; motion releases+tracks+refreezes cleanly. Also un-broke Lock (steady gate now latches SURFACE READY). **2026-07-02 REAL fix (`c7442c2`+`9887c5e`, pushed, backend-only, LIVE-VERIFIED via `/ws` tap):** measured raw jitter with the arm PARKED = X ~83 mm / A(edge) ~73° / B/C ~2-4° = pure RealSense fit noise; the client freeze never latched (too noisy). Now the backend HOLDS all pose-derived readouts gated on the actual RoboDK camera pose (mirrors the arm) + a vision escape (12 mm/4°) so a real dolly still tracks even if the driver isn't monitoring. Verified live: parked → every axis 0.000 peak-to-peak (held=31/31). **2026-07-06 RANGE-target moving-goalpost FIXED (`eb523cf`, pushed, host-only, 204 tests green):** the distance target jumped 592→300 when the operator moved toward it ("can never reach it") — it was recomputed each frame as `fit*frame_margin(1.05)` sitting ON the crop edge, so nudging closer flipped to crop and slammed `ideal_distance` to `accurate_min(300)`, driving them even closer. Now crop HOLDS the target latched while framed (only never-framed/oversized → accurate_min); `frame_margin` 1.05→1.12 moves the aim point inside the frame. Real goal (edges captured + on-cell validation, handoff §4 / P1-P4) still pending on-cell. Plan: `~/.claude/plans/gleaming-nibbling-kitten.md`. **2026-07-06 SAM point-prompted live boundary DONE + live-verified (`744f41c`, host-only, 217 tests):** `sam_boundary.py` (model-agnostic ONNX, background worker, shared `mask_to_boundary`); `boundary_engine=sam_then_color` default; EdgeSAM hugged the mat score 0.985 where colour abstained; live `/ws` boundary ~1/s contrast=0.835. LICENSE: EdgeSAM=non-commercial (MobileSAM Apache swaps in via config). Weights gitignored → `pip install -e .[sam]`+`tools/download_sam.py`. WINDOWS FIX: onnxruntime pre-loaded in `tasni/__init__.py` before robolink/Qt (shiboken2 hook breaks its DLL init otherwise). **2026-08-13 live-cell Defect 2 (~10 s HUD lag) INSTRUMENTED not diagnosed (`eb12a13`, host-only, 426 tests):** `live_diag.py` `LiveLatencyProbe` logs loop/RPC/telemetry-cadence/age/held every 5 s (`scan.live_latency_log_s`); read it on the next cell run and match the signature table in `docs/scan-workframe-cell-defects.md` BEFORE fixing. Latent hazard found: the 2 s staleness gate subtracts a JETSON-stamped time from the HOST clock (no RTC battery → skew freezes the HUD while video streams). **2026-08-30 `2850d19`: "perfect lock, rejected scan" — the locked rect took the lock's SIZE but the camera AIM's centre/yaw (17 mm off), failing the +X edge gate; asymmetric edge support = right size, wrong place.**
- [Cell characterization 2026-08-13](cell-characterization-2026-08-13.md) — measured depth envelope; incidence costs ~4x what distance costs; the numbers Milestone B must be built from.
- [Don't run the full pytest suite](pytest-suite-too-slow-to-run-fully.md) — too slow; user interrupted it twice. Use import/syntax checks + `pytest -k` on the specific tests, and `npm run build` for the frontend.
- [Scan survey/planner plan](scan-survey-planner-plan.md) — surface-aware standoff/cone/count + live aiming grid: IMPLEMENTED + pushed (`origin/calibration-improvements` `e00f43c`, 2026-06-24). Spec in `docs/scan-survey-planner-handoff.md`.
- [Workframe two-path plan](workframe-two-path-plan.md) - design doc reviewed+revised and 17-task implementation plan written (45314f6, pushed); awaiting execution by another agent.
- [Branch merged to main 2026-08-25](branch-merged-to-main-2026-08-25.md) — `calibration-improvements` merged (`51849b1`) + deleted locally; Jetson re-pointed to `main`; CLAUDE.md's "today's branch" line is now stale.
- [Jetson laser power vs characterization](jetson-laser-power-vs-characterization.md) — RESOLVED: pinned to 150 via `Environment=` in the unit, read-back verified. Lesson: RealSense option state lives on the DEVICE, so "leave alone" preserves whatever was last written.
- [Extrusion A4 wrist-flip fix](extrusion-a4-wrist-flip-fix.md) — CYLINDER RESOLVED + MERGED to main `534b9b5` (2026-08-27, operator: "perfect and smooth"). Cause = the path-to-tool SEED (RoboDK mirrors the seeded roll), not the tool; native program, zero targets. Mirrored-TCP is a no-op; `.src` import blocked by a modal dialog. The INSPECTION move had the SAME 180°-convention bug — fixed + MERGED to main `9ed159c` (2026-08-27). Both CELL-VALIDATED by the operator 2026-08-27 ("all good now"). CLOSED. Cell dry-run still pending.
- [Windows python & encoding traps](windows-python-and-encoding-traps.md) — use `py -3.10` (no `python` on PATH); never round-trip source through PowerShell Get-Content/Set-Content — it silently mojibakes this repo's UTF-8.
- [Restart Tasni backend after code edits](restart-tasni-backend-after-code-edits.md) — the app caches imported modules; check process StartTime vs file mtime before asking for any cell test, or you test stale code.
- [PFH paper ring-stack experiment](pfh-paper-ring-stack-experiment.md) — deadline 1 Sep 2026; run is now noise floor (5 unattended trips) → re-place → rings 2-3 true → ring 4 placed off-centre; parked-vs-re-approach repeatability split, taught-target side photo, and the pairing fallback to the ring beneath. **RUN-DAY: branch guard is ~1 px from aborting (reprocess, don't re-run the robot, don't loosen it); measure at ~300 mm not 448; more frames/pose buy nothing.**
- [Extrusion take figures](extrusion-take-figures.md) — plan/heightmap/mesh/iso/profile/pipeline + stack/tube rendered from the archive with no robot; traps: deposit-band colour range, FITTED-not-averaged centre, mesh pitch vs cloud spacing, box-aspect faking the stated exaggeration, and the archived cloud being the crest not the bead.
- [UX overhaul spec](ux-overhaul-spec.md) — 2026-08-28 design spec for the stepped-workflow shell + cell-level connect + journey dashboard, pushed on `ux-overhaul` (`3f26c7f`, 3 review rounds folded in); AWAITING operator go, then writing-plans per phase; phase 3 (Extrusion) not before the paper ring-stack cell run.
- [Jetson wifi & network ops](jetson-wifi-network-ops.md) — Intel 8265 not a dongle; In5 = WPA2-PSK, no portal/isolation; polkit blocks nmcli over SSH (use sudo -S via stdin); back-to-back wifi switches race into a bogus "802.1X supplicant failed".
- [Live print dispatch blocker](live-print-dispatch-blocker.md) — RESOLVED 2026-08-28 per operator (`4ac5c3f`); kept for `tools/dispatch_bisect.py` and the evidence-first lesson (a day lost to confident diagnoses from partial evidence).
- [Multi-view inspection spec + plan](multiview-inspection-spec.md) — **IT WAS BUILT**: 17 commits / +3659 lines on `origin/worktree-multiview-inspection` @ `96a17f6`, never merged, never run on the cell. 45 commits behind main; its "1 mm voxel" note is stale; it holds the `depth_plane_check` incidence fix main still lacks (main refuses any tilt past ~18° at 300 mm). ChArUco BANNED; registration is ring-first.
- [Jetson USB camera failure](jetson-usb-camera-failure.md) — "connected but no image": EPROTO -71 → tegra-xusb HC death → needs a physical replug; grab dmesg EARLY, the spam floods the ring buffer.
- [Camera server recovery supervisor](camera-server-recovery-supervisor.md) — `read_frames()` rebuilds a wedged pipeline instead of serving nothing (`267cf71`); two bounds exist so it can't hammer USB.
- [Sensor layer full-fidelity plan](sensor-layer-full-fidelity-plan.md) — SHIPPED + merged to main 2026-08-30 (`54ab1da`+`cdc91f8`+`f6c17e6`), Jetson live-verified; `py -3.10 tools/cell_health.py` is the morning readiness command; CUDA IS on the hot path but the depth filters have no GPU path at all; all 4 review findings + both greeting-coherence defects FIXED + deployed (`0c3a2b0`/`4319c9e`/`697cd84`); a rebuild mid-burst now closes the connection and scan falls back to per-pose, which RE-TOURS the robot.
- [Extrusion platform centring](extrusion-platform-centring.md) — centring resolves the platform middle from the STATION (`Tasni Work Center` frame, else the `Tasni Work Surface` mesh) before `runs/scan/active.json`, expressed in whichever work frame is selected; shipped `1cceb3c`, live-verified on the cell.

- [First live take: board halo](first-live-take-board-halo.md) — 2026-08-31 first live take under geometry segmentation ABORTED on the branch guard; 32 board points at 2.9-3.8 sigma (stereo edge halo) that the deleted chroma gate used to remove. Pre-merge A/B on the same frame passes. Do not loosen the guard. **Roll probe 17:12 came back VALID but WORSE (separation margin -0.119, sigma 0.866, floor pinned) — and `separation_margin_mm` gated nothing — FIXED `95595a8`.**

- [Crest height shortfall](crest-height-shortfall.md) — camera reads ring height ~1.5 mm LOW (r=0.97 on shape) vs the operator's ruler; eats a 1.78 mm floor with 0.13 mm headroom. Suspect: spatial filter smooth_delta 20 vs a ring spanning <4 disparity px. A/B levers shipped `f9c4a53`, deployed.

- [Depth pimples: census + preset landmine](depth-pimples-census-and-preset-landmine.md) — noise is STATIC (1.176 spatial vs 0.130 temporal) and has a distance-INDEPENDENT floor: `sigma = 0.662 + 2.196e-6*Z^2`, so 77% of it at 300 mm cannot be fixed by moving closer. Texture gating is fully off, census window is 9x9. Z^1.72 was a metric artifact, dissolved. Never `load_json` the committed as-found preset — it carries `depthunits=1000` and would silently undo R2.

- [Extrusion voxel downsample](extrusion-voxel-downsample.md) — 1.0 -> 0.5 mm 2026-08-31: 1 mm was deleting marginal deposits and reporting a continuous ring as open (0.875 -> 0.9925, no camera change). Neutral on good takes, consistently worse on partial ones. Synthetic renderer CANNOT reproduce it; the test is archive-gated.

- [Extrusion debug images mirrored](extrusion-debug-images-mirrored.md) — segmentation/skeleton/comparison.png were left-right mirrored vs color.png (camera +X points along work -X); fixed at write time 2026-08-31. Archives written BEFORE the fix are NOT flipped.

- [Radial trim fixed point](radial-trim-fixed-point.md) — the trim schedule stopped while its circle fit was still moving, letting a SOLID board patch ride the last band to r+band; now iterates the tightest band to a fixed point (`50d0b34`). Residual: leaks again at patch half-height ±22 mm.

- [Layer 2 stack segmentation](layer2-stack-segmentation.md) — layer >=2 never measured validly (0/6 takes): assembly was off above layer 1 so the largest 110° arc became "the ring". Fixed by a deposit floor under layer N + assembly everywhere. Height cut ALONE is a no-op; the rings do NOT separate by height in the deposit cloud.

- [Tasni backend native crash](tasni-backend-native-crash.md) — SOLVED 2026-09-01 (`bcd05cd`): two OpenBLAS runtimes (numpy's `openblas64_` + scipy's own copy) multithreading in one process. Found via the **Windows Application-Error log**, not faulthandler — read that first for any native crash.

- [RealSense runtime SET](realsense-runtime-set.md) — the camera server takes a runtime `SET` for the FILTER CHAIN (no deploy per A/B arm); merged + deployed + live-verified 2026-09-01. Device options stay pinned in the unit file by design; an override dies on restart.

- [Unsettled burst has no static field](unsettled-burst-has-no-static-field.md) — the FIRST burst at a new pose doesn't correlate with itself (+0.045 vs +0.937 settled); two settled bursts agree at +0.960. Any static-structure diagnostic needs a settled burst.

- [Roll probe: camera vs scene](roll-probe-camera-vs-scene.md) — the one-excursion test that separates a stereo artifact from real geometry, and the premise the whole multi-view case rests on. ATTEMPTED 2026-08-31 and REFUSED (the roll itself moved the noise floor — IR dot pattern vs printed board); protocol redesigned 2026-09-01 to a SYMMETRIC ±30° pair (`82358c8`), then tightened by review to spatial-filter A/B first + A-B-A with drift control (`95f0fb0`) — the old pair was ALSO viewpoint-confounded (bead recipe moved the camera 6.1 mm, verified).

- [Roll verdict: vertical flattens the bead](roll-verdict-vertical-flattens-bead.md) — 2026-09-01 paired run ANSWERS the roll probe: the dropout moves with the camera. Vertical reads 0.6-2.4 mm where rolled reads 5.0-9.7; 8/8 vertical invalid, 8/8 rolled valid. Size gap is ARC COVERAGE (±3-4 mm), not scale. The rolled view is lucky, not right.

- [apply-characterization ignores validity](apply-characterization-ignores-validity.md) — it takes the VERTICAL view even at `valid:false`; the 17:43 run ran on a 50.1 mm nominal ring that should have been 42.4, centred 9 mm off. Its `mean_absolute_mm`/`center_offset_norm_mm` are meaningless.
